<VirtualHost *:80>
    ServerName localhost
    DocumentRoot /var/www/html/public

    <Directory /var/www/html/public>
        AllowOverride All
        Require all granted
    </Directory>

    # Handle Static Assets
    Alias /subscription/build /var/www/html/public/subscription/build
    <Directory "/var/www/html/public/subscription/build">
        AllowOverride None
        Require all granted
        ExpiresActive On
        ExpiresDefault "access plus 1 year"
        Header set Cache-Control "public"
    </Directory>

    Alias /subscription/assets /var/www/html/public/subscription/assets
    <Directory "/var/www/html/public/subscription/assets">
        AllowOverride None
        Require all granted
        ExpiresActive On
        ExpiresDefault "access plus 1 year"
        Header set Cache-Control "public"
    </Directory>

    # Security - Block Hidden Files
    <FilesMatch "^\\.">
        Require all denied
    </FilesMatch>

    ErrorLog ${APACHE_LOG_DIR}/error.log
    CustomLog ${APACHE_LOG_DIR}/access.log combined
</VirtualHost>